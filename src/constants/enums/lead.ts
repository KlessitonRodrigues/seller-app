export enum LEAD_STATUS {
  REJECTED = "rejected",
  PENDING = "pending",
  APPROVED = "approved",
}

export enum OPPORTUNITY_STAGE {
  APPROVED = "approved",
  REJECTED = "rejected",
  PENDING = "pending",
}
